# PillSafe — Python Dependencies
# Install: pip install -r requirements.txt --break-system-packages

# Core
opencv-contrib-python==4.13.0.92  # OpenCV with Haar Cascade face detection (NumPy 2.x compatible)
numpy>=2.0.0
scipy>=1.12.0                     # Cosine distance for FaceNet embedding comparison

# FaceNet / MobileFaceNet inference
# On Raspberry Pi:  pip install tflite-runtime --break-system-packages
# On dev machine:   pip install tensorflow --break-system-packages
# Only ONE of the above is needed — the code auto-detects which is available.
tflite-runtime>=2.14.0; platform_machine == "aarch64"   # Pi 4/5 (ARM64)
# tensorflow>=2.15.0              # Uncomment for x86 dev/testing

# Camera (Raspberry Pi — pre-installed on Pi OS)
# picamera2  # Already available on Raspberry Pi OS

# Hardware interfaces
#RPi.GPIO>=0.7.1                   # GPIO control for servo, buzzer, IR sensors
smbus2>=0.4.3                     # I2C communication with DS3231 RTC

# Serial communication
pyserial>=3.5                     # UART control of SIM800L GSM module

# Web framework
Flask>=3.0.0                      # REST API server

# Configuration
PyYAML>=6.0                       # config.yaml loader

# Voice authentication (optional — the system degrades gracefully if missing)
# Required by core/voice_recogniser.py for I2S mic capture + MFCC features.
sounddevice>=0.4.6               # Audio capture from the I2S/INMP441 microphone
librosa>=0.10.0                  # MFCC feature extraction for speaker verification

# Development / testing only (not needed on the Pi at runtime)
# matplotlib>=3.8.0              # Used by test_facial_verification.py preview plots
# pytest>=8.0                    # Unit tests in tests/  (pytest -q)
